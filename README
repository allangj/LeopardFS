The makefile assumes the directory /opt/arm-2009q1 exists and contains the toolchain

The make file assumes the kernel is in the same directory as the makefile

It is assumed that quilt is installed, if not if is required you do it
